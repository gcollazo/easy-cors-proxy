{
  "name": "Easy CORS Proxy",
  "description": "Proxy any URL with added CORS headers",
  "repository": "https://github.com/gcollazo/easy-cors-proxy",
  "keywords": ["node", "cors", "proxy"],
  "env": {
    "NODE_ENV": "production",
    "PROXY_URL": {
        "description": "The URL of the server you want to proxy",
        "value": "http://example.com:80"
    }
  }
}
